
Add a nice menu bar with popover modal menus?

Will jQuery ajax call read "foreign" URLs instead of proxy.pl?

Include Flot

Separate Sorting from Updating 'output' so can export sort'd votable
	sorting in voview.xsl produces html
	would have to create new xsl for sorting export

Opera has a problem using the whole proxy thing

Data export should be available by default; the code is already in place; inputs
to use the code should be included in the default HTML.

The whole code base could be better documented.  Comments explaning the 
purpose of each function and method would be most helpful.
